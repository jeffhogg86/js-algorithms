const bubbleSort = (arr: number[]): number[] => {
  const swap = (arr, i, j) => {
    [arr[i], arr[j]] = [arr[j], arr[i]];
  };

  return arr;
};

const selectionSort = (arr: number[]): number[] => {
  return arr;
};

bubbleSort([14, 5, 7]);
