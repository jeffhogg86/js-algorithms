const linearSearch = (arr: number[], val: number): number => {
  return arr.indexOf(val);
};

const searchBinary = (arr, val) => {};

const strSearch = (str: string, target: string): number => {
  let count = 0;
  str.split('').forEach(char => {});
  return 0;
};
